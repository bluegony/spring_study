<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.sql.Post">
	<insert id="createZip" parameterType="Post">
		INSERT INTO 
		POST(SEQ, ZIPCODE, SIDO, GUGUN, DONG, RI, ST_BUNJI, ED_BUNJI) 
		VALUES (#{seq},#{zipcode},#{sido},#{gugun},#{dong},#{ri},#{stBunji},#{edBunji})
	</insert>
	
</mapper>	